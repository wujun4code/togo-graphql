# Togo Graphql

## Develop locally

### Use docker to start dependent components

```sh
docker compose up -d
```

### run NodeJS service

```sh
npm install
npm run migrate:deploy
npm run generate
npm run dev
```